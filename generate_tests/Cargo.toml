# generate_tests/Cargo.toml 

[package]
name = "generate_tests"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
yaml_serde = "0.10.7"
hex = "0.4.3"
rustdns = { path = "../", default-features = false, features = ["udp"] }


[[bin]]
name = "generate_tests"
path = "main.rs"
